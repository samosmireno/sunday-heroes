/** The mark on a Fixture that was still not completed when its Season closed. */
export default function NotCompletedTag() {
  return (
    <span className="rounded bg-amber-900/30 px-1.5 py-0.5 text-[10px] font-medium uppercase tracking-wide text-amber-400">
      Not completed
    </span>
  );
}
